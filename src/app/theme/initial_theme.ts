import { Theme } from "@/definitions/theme"

export const initial_theme: Theme = {
    colors: {
        primary: "#37123C",
        secondary: "#6564DB",
        text: {
            primary: "#37123C"
        }
    }
}
